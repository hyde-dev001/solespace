# 📊 ARCHITECTURE & VISUAL DIAGRAMS

Complete visual representations of the integration.

---

## 🏗️ SYSTEM ARCHITECTURE

```
┌─────────────────────────────────────────────────────────────────┐
│                    SHOP OWNER REGISTRATION SYSTEM               │
└─────────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────┐
│      FRONTEND (React + TypeScript)     │
│      localhost:5173                    │
├────────────────────────────────────────┤
│                                        │
│  ┌─────────────────────────────────┐  │
│  │    SignUpForm Component         │  │
│  │                                 │  │
│  │  - Form Fields (8 inputs)       │  │
│  │  - Form State (formData)        │  │
│  │  - Validation Logic             │  │
│  │  - Success/Error Messages       │  │
│  │  - Loading State                │  │
│  │                                 │  │
│  └────────────┬────────────────────┘  │
│               │ handleSubmit()        │
│               │ calls                 │
│  ┌────────────▼────────────────────┐  │
│  │   shopRegistrationApi Service   │  │
│  │                                 │  │
│  │  registerShopOwner()            │  │
│  │  - Takes form data              │  │
│  │  - Makes fetch request          │  │
│  │  - Handles response             │  │
│  │                                 │  │
│  └────────────┬────────────────────┘  │
│               │ POST Request          │
└───────────────┼──────────────────────┘
                │
        ═══════════════════  HTTP
                │
        POST /api/shop/register
        JSON Content-Type
                │
┌───────────────▼──────────────────────┐
│   BACKEND (Laravel + PHP)            │
│   127.0.0.1:8000                     │
├────────────────────────────────────┤
│                                    │
│ ┌──────────────────────────────┐  │
│ │  Route: POST /api/shop/...   │  │
│ │  Uses: Middleware CORS       │  │
│ │  Routes to: Controller       │  │
│ └──────────────┬───────────────┘  │
│                │                  │
│ ┌──────────────▼───────────────┐  │
│ │ ShopRegistrationController   │  │
│ │                              │  │
│ │ store() Method               │  │
│ │  - Validate Data             │  │
│ │  - Check Email Unique        │  │
│ │  - Try-Catch Block           │  │
│ │                              │  │
│ └──────────────┬───────────────┘  │
│                │                  │
│ ┌──────────────▼───────────────┐  │
│ │  ShopOwner Model             │  │
│ │                              │  │
│ │  ShopOwner::create([data])   │  │
│ │  - Map data to columns       │  │
│ │  - Save to database          │  │
│ │                              │  │
│ └──────────────┬───────────────┘  │
│                │                  │
└────────────────┼──────────────────┘
                 │
        ═══════════════════  HTTP
                 │
        JSON Response 201/500
                 │
┌────────────────▼──────────────────┐
│   DATABASE (MySQL)                │
│   shoe_store                       │
├───────────────────────────────────┤
│                                   │
│  shop_owners table                │
│  ├── id                           │
│  ├── first_name                   │
│  ├── last_name                    │
│  ├── email (unique)               │
│  ├── phone                        │
│  ├── business_name                │
│  ├── business_address             │
│  ├── business_type                │
│  ├── registration_type            │
│  ├── operating_hours (json)       │
│  ├── status (pending)             │
│  ├── created_at                   │
│  └── updated_at                   │
│                                   │
└───────────────────────────────────┘
```

---

## 🔄 REGISTRATION DATA FLOW

```
START
  │
  ▼
┌──────────────────────────────────────┐
│  User Opens Sign Up Page             │
│  - Sees form with 8 fields           │
│  - Checkbox for terms                │
└──────────┬───────────────────────────┘
           │
           ▼
┌──────────────────────────────────────┐
│  User Fills Form Fields              │
│  - firstName, lastName, email, phone │
│  - businessName, businessAddress     │
│  - businessType, registrationType    │
└──────────┬───────────────────────────┘
           │
           ▼
┌──────────────────────────────────────┐
│  User Checks Terms & Conditions      │
└──────────┬───────────────────────────┘
           │
           ▼
┌──────────────────────────────────────┐
│  User Clicks "Sign Up" Button        │
└──────────┬───────────────────────────┘
           │
           ▼
┌──────────────────────────────────────┐
│  handleSubmit() Function Runs        │
│  ✓ Check all fields filled           │
│  ✓ Check checkbox checked            │
│  ✓ Set isLoading = true              │
└──────────┬───────────────────────────┘
           │
           ▼
┌──────────────────────────────────────┐
│  registerShopOwner(formData) Called  │
│  - Receives form data object         │
│  - Prepares JSON body                │
└──────────┬───────────────────────────┘
           │
           ▼
┌──────────────────────────────────────┐
│  fetch() to POST /api/shop/register  │
│  - URL: http://127.0.0.1:8000        │
│  - Headers: application/json         │
│  - Body: form data as JSON           │
└──────────┬───────────────────────────┘
           │
           ▼ (Network Request)
           │
┌──────────────────────────────────────┐
│  Backend Receives Request            │
│  ShopRegistrationController::store() │
└──────────┬───────────────────────────┘
           │
           ▼
┌──────────────────────────────────────┐
│  Validate Data                       │
│  ✓ firstName: required|string        │
│  ✓ lastName: required|string         │
│  ✓ email: required|email|unique      │
│  ✓ phone: required|string            │
│  ✓ businessName: required|string     │
│  ✓ businessAddress: required|string  │
│  ✓ businessType: required|string     │
│  ✓ registrationType: required|string │
└──────────┬───────────────────────────┘
           │
    ┌──────┴──────┐
    │             │
    ▼             ▼
  PASS         FAIL
    │             │
    │      ┌──────▼─────────┐
    │      │ Return Error   │
    │      │ JSON 400/422   │
    │      │ success: false │
    │      └────────┬───────┘
    │             │
    │      ┌──────▼─────────┐
    │      │ Send to        │
    │      │ Frontend       │
    │      │ handleSubmit() │
    │      └────────┬───────┘
    │             │
    │      ┌──────▼─────────┐
    │      │ setError()     │
    │      │ Display error  │
    │      │ message        │
    │      └────────────────┘
    │
    ▼
┌──────────────────────────────────────┐
│  Try Block                           │
│  Create ShopOwner Record             │
│  ShopOwner::create([                 │
│    'first_name' => ...,              │
│    'last_name' => ...,               │
│    'email' => ...,                   │
│    'phone' => ...,                   │
│    'business_name' => ...,           │
│    'business_address' => ...,        │
│    'business_type' => ...,           │
│    'registration_type' => ...,       │
│    'status' => 'pending'             │
│  ])                                  │
└──────────┬───────────────────────────┘
           │
    ┌──────┴──────┐
    │             │
    ▼             ▼
  SUCCESS      EXCEPTION
    │             │
    ▼             ▼
┌────────┐  ┌──────────────┐
│Return  │  │Catch error   │
│JSON 201│  │Return JSON   │
│success:│  │500 error     │
│true    │  │success: false│
└────┬───┘  └──────┬───────┘
     │             │
     └──────┬──────┘
            │
            ▼
┌──────────────────────────────────────┐
│  Send Response to Frontend           │
│  JSON:                               │
│  {                                   │
│    "success": true/false,            │
│    "message": "...",                 │
│    "data": { shop_owner }            │
│  }                                   │
└──────────┬───────────────────────────┘
           │
           ▼ (Network Response)
           │
┌──────────────────────────────────────┐
│  Frontend Receives Response          │
│  handleSubmit() in try-catch         │
└──────────┬───────────────────────────┘
           │
    ┌──────┴──────┐
    │             │
    ▼             ▼
  SUCCESS      ERROR
    │             │
    ▼             ▼
┌────────┐  ┌────────────┐
│Success │  │setError()  │
│message │  │Display     │
│setSuccess │ error msg │
│           │ Keep form │
│Reset form │ filled    │
│Clear data │            │
└────────┘  └────────┬──┘
     │               │
     │        ┌──────▼──┐
     │        │isLoading│
     │        │= false  │
     │        └─────────┘
     │
     ▼
┌──────────────────────────────────────┐
│  setIsLoading = false               │
│  Button enabled again               │
└──────────┬───────────────────────────┘
           │
           ▼
┌──────────────────────────────────────┐
│  User Sees Success Message           │
│  Form is Reset                       │
│  Can Submit Again or Navigate        │
└──────────┬───────────────────────────┘
           │
           ▼
       CHECK
     DATABASE
       Table
   shop_owners
    See new
    record!
           │
           ▼
         END
```

---

## 📁 FILE DEPENDENCY DIAGRAM

```
FRONTEND
│
├─ src/components/auth/SignUpForm.tsx
│  ├─ imports: useState
│  ├─ imports: Link
│  ├─ imports: EyeCloseIcon, EyeIcon
│  ├─ imports: Label, Input, Checkbox
│  └─ imports: registerShopOwner from "shopRegistrationApi"
│
└─ src/services/shopRegistrationApi.ts
   └─ exports: registerShopOwner function


BACKEND
│
├─ routes/web.php
│  └─ references: ShopRegistrationController
│
├─ bootstrap/app.php
│  └─ configures: Middleware
│
├─ app/Http/Controllers/ShopRegistrationController.php
│  ├─ imports: ShopOwner Model
│  ├─ imports: ShopDocument Model
│  └─ imports: Request
│
├─ app/Models/ShopOwner.php
│  └─ table: shop_owners
│
├─ app/Models/ShopDocument.php
│  └─ table: shop_documents
│
└─ database/migrations/
   └─ 2026_01_14_205002_create_shop_owners_table.php
      └─ creates: shop_owners table


DATABASE
│
└─ MySQL
   └─ Database: shoe_store
      ├─ Table: shop_owners
      └─ Table: shop_documents
```

---

## 🔗 API ENDPOINT DIAGRAM

```
REQUEST:
┌─────────────────────────────────────┐
│ POST /api/shop/register              │
├─────────────────────────────────────┤
│ Headers:                            │
│  Content-Type: application/json     │
│  Accept: application/json           │
├─────────────────────────────────────┤
│ Body (JSON):                        │
│ {                                   │
│   "firstName": "John",              │
│   "lastName": "Doe",                │
│   "email": "john@example.com",      │
│   "phone": "1234567890",            │
│   "businessName": "John's Shop",    │
│   "businessAddress": "123 Main St", │
│   "businessType": "retail",         │
│   "registrationType": "sole_...     │
│ }                                   │
└─────────────────────────────────────┘


RESPONSE (Success - 201):
┌─────────────────────────────────────┐
│ Status: 201 Created                 │
├─────────────────────────────────────┤
│ {                                   │
│   "success": true,                  │
│   "message": "Shop registration..." │
│   "data": {                         │
│     "id": 1,                        │
│     "first_name": "John",           │
│     "last_name": "Doe",             │
│     "email": "john@example.com",    │
│     "phone": "1234567890",          │
│     "business_name": "John's Shop", │
│     "status": "pending",            │
│     "created_at": "2026-01-15T...", │
│     "updated_at": "2026-01-15T..."  │
│   }                                 │
│ }                                   │
└─────────────────────────────────────┘


RESPONSE (Error - 500):
┌─────────────────────────────────────┐
│ Status: 500 Internal Server Error   │
├─────────────────────────────────────┤
│ {                                   │
│   "success": false,                 │
│   "message": "Error description"    │
│ }                                   │
└─────────────────────────────────────┘
```

---

## 🧩 COMPONENT INTERACTION DIAGRAM

```
┌─────────────────────────────────┐
│   SignUpForm Component          │
│   (Frontend)                    │
└──────────────┬──────────────────┘
               │
        Uses State:
        ├─ formData
        ├─ isLoading
        ├─ error
        ├─ success
        ├─ isChecked
        └─ showPassword
               │
               ▼
        ┌────────────────────┐
        │ User Input Events  │
        │ onChange()         │
        └────────┬───────────┘
                 │
                 ▼
        ┌────────────────────┐
        │ handleInputChange()│
        │ Updates formData   │
        └────────┬───────────┘
                 │
        onSubmit Event
                 │
                 ▼
        ┌────────────────────┐
        │ handleSubmit()     │
        │ ├─ Validates      │
        │ ├─ Calls API      │
        │ └─ Updates State  │
        └────────┬───────────┘
                 │
                 ▼
        ┌────────────────────────────┐
        │ registerShopOwner()        │
        │ (API Service Function)     │
        │                            │
        │ ├─ fetch() POST request   │
        │ ├─ Sends JSON            │
        │ └─ Returns Promise       │
        └────────┬───────────────────┘
                 │
        Promise resolves
                 │
                 ▼
        ┌────────────────────┐
        │ Update State:      │
        │ ├─ setSuccess()   │
        │ ├─ setError()     │
        │ ├─ Reset formData │
        │ └─ setIsLoading() │
        └────────┬───────────┘
                 │
                 ▼
        ┌────────────────────┐
        │ Re-render Form    │
        │ Show message      │
        │ Display state     │
        └────────────────────┘
```

---

## 📊 STATE MANAGEMENT DIAGRAM

```
SignUpForm Component State:

formData: {
  ├─ firstName: string
  ├─ lastName: string
  ├─ email: string
  ├─ phone: string
  ├─ businessName: string
  ├─ businessAddress: string
  ├─ businessType: string
  └─ registrationType: string
}

isLoading: boolean
  └─ Controls button disabled state

error: string
  └─ Displays error message

success: string
  └─ Displays success message

isChecked: boolean
  └─ Checkbox validation

showPassword: boolean
  └─ Password visibility toggle


Updates happen via:
├─ handleInputChange() → formData
├─ handleSubmit() → isLoading, error, success
└─ API Response → error or success
```

---

**Last Updated:** January 15, 2026 ✅
